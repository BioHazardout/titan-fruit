import Image from "next/image";
import { Button } from "@/components/ui/button";
export function PageHero({ title, description, image, eyebrow, fullScreen = false }: { title: React.ReactNode; description: string; image: string; eyebrow: string; fullScreen?: boolean }) { const sectionHeight = fullScreen ? "h-[calc(100svh-60px)] min-h-[590px] lg:min-h-0" : "min-h-[490px] sm:min-h-[530px]"; const contentHeight = fullScreen ? "h-full" : "min-h-[490px] sm:min-h-[530px]"; return <section className={`relative isolate overflow-hidden bg-forest text-white ${sectionHeight}`}><Image src={image} priority fill alt="Temporary premium fresh produce photography" sizes="100vw" className="object-cover object-center"/><div className="absolute inset-0 bg-gradient-to-r from-[#021e10]/95 via-[#021e10]/72 to-[#021e10]/12"/><div className={`container relative flex items-center py-16 ${contentHeight}`}><div className="max-w-2xl"><p className="eyebrow text-lime">{eyebrow}</p><h1 className="display mt-5 text-5xl font-bold uppercase sm:text-6xl lg:text-7xl">{title}</h1><div className="mt-6 h-0.5 w-20 bg-lime"/><p className="mt-5 max-w-lg text-base leading-7 text-white/80">{description}</p><div className="mt-7"><Button href="#products">VIEW PRODUCTS</Button></div></div></div></section>; }
