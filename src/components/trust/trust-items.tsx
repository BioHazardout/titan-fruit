import { Icon, type IconName } from "@/components/ui/icons";
const trust: { icon: IconName; title: string; text: string }[] = [
  { icon:"shield", title:"Quality Assured", text:"Rigorous quality control at every stage of the process." }, { icon:"certificate", title:"SENASA Authorized", text:"Certified primary-processing facility." }, { icon:"spark", title:"Food Safety", text:"International food safety standards and traceability systems." }, { icon:"leaf", title:"Sustainable", text:"Committed to responsible agriculture and the environment." }, { icon:"globe", title:"Global Expertise", text:"Proven track record in international markets with a reliable supply chain." },
];
export function TrustItems({ dark = false }: { dark?: boolean }) { return <div className="grid gap-y-8 sm:grid-cols-2 lg:grid-cols-5">{trust.map((item, index) => <div key={item.title} className={`relative px-5 text-center transition-none hover:transform-none hover:shadow-none first:pl-0 last:pr-0 ${index > 0 ? (dark ? "lg:border-l lg:border-white/15" : "lg:border-l lg:border-forest/15") : ""}`}><Icon name={item.icon} className={`mx-auto h-8 w-8 ${dark ? "text-lime" : "text-leaf"}`}/><h3 className={`mt-4 text-[12px] font-bold uppercase tracking-[.08em] ${dark ? "text-white" : "text-forest"}`}>{item.title}</h3><p className={`mx-auto mt-2 max-w-[200px] text-[14px] leading-5 ${dark ? "text-white/75" : "text-ink/60"}`}>{item.text}</p><span className="mx-auto mt-4 block h-0.5 w-10 bg-lime"/></div>)}</div>; }
