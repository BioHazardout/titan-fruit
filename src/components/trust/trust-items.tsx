import { Icon, type IconName } from "@/components/ui/icons";
const trust: { icon: IconName; title: string; text: string }[] = [
  { icon:"shield", title:"Quality Assured", text:"Strict quality control at every step." }, { icon:"certificate", title:"SENASA Authorized", text:"Authorized primary-processing facility." }, { icon:"spark", title:"Food Safety", text:"International food safety standards and traceability systems." }, { icon:"leaf", title:"Sustainable", text:"Committed to responsible agriculture and the environment." }, { icon:"globe", title:"Global Expertise", text:"Experience in international markets and reliable supply chain." },
];
export function TrustItems({ dark = false }: { dark?: boolean }) { return <div className="grid gap-y-8 sm:grid-cols-2 lg:grid-cols-5">{trust.map((item, index) => <article key={item.title} className={`relative px-5 text-center first:pl-0 last:pr-0 ${index > 0 ? "lg:border-l lg:border-forest/15" : ""}`}><Icon name={item.icon} className={`mx-auto h-8 w-8 ${dark ? "text-lime" : "text-leaf"}`}/><h3 className="mt-4 text-[11px] font-bold uppercase tracking-[.08em]">{item.title}</h3><p className={`mx-auto mt-2 max-w-[190px] text-[13px] leading-5 ${dark ? "text-white/65" : "text-ink/60"}`}>{item.text}</p><span className="mx-auto mt-4 block h-0.5 w-10 bg-lime"/></article>)}</div>; }
